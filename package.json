{
  "name": "flyby-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bun run check && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit && tsc --noEmit -p tsconfig.tools.json && bun tools/verify-packs.ts",
    "bake": "bun tools/bake-city.ts",
    "verify": "bun tools/verify-packs.ts"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "bun-types": "^1.4.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
